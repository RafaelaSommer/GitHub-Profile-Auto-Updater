# ============================================
# WORKFLOW DESATIVADO TEMPORARIAMENTE
# ============================================
# Motivo: evitar execuções automáticas
#
# Para reativar:
# 1. Descomente o bloco "on"
# 2. Ajuste o cron se necessário
# ============================================

name: Atualizar README do Perfil (DESATIVADO)

# on:
#   schedule:
#     - cron: "*/10 * * * *"
#   workflow_dispatch:

permissions:
  contents: write

jobs:
  atualizar-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Workflow desativado
        run: |
          echo "🚫 GitHub Actions desativado"
          echo "Nenhuma ação será executada"
